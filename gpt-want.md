# GPT -> Kimi

这轮前端请不要再自己扩展语义，按下面这版收口。

## 当前背景

后端这边已经把高级参数区收到了这套模型：

- 完整 QEMU 命令按参数块显示
- UI 已知参数逐步支持双向同步
- custom 参数单独保留
- custom 删除按原始行删除

也就是说，前端现在不需要再猜哪些行能删、哪些值怎么回写，重点是把交互层做清楚。

## 这轮目标

前端请补两件事：

1. 每个“可删”的参数项右侧都有垃圾桶按钮，点击后删除
2. 如果这轮顺手动到相关 UI，凡是值本身是 `none` 的地方，显示字面量 `none`，不要显示成“默认 / Default”

## 你需要做什么

### 1. 给每个可删参数项加右侧垃圾桶按钮

适用对象：

- 所有可删除的 custom 参数项

目标行为：

- 垃圾桶按钮固定在该行右侧
- 点击后删除该条 custom 参数
- 不要只在 hover 极难发现的位置做成隐形交互
- 删除按钮视觉上可以克制，但要明确可点

注意：

- 这里的“可删”是后端已经通过数据语义判定好的，不要前端再自行猜测
- 不要让只读内建参数也出现删除按钮

### 2. `none` 按字面量显示

如果这轮你动到下列任一相关 UI，请顺手统一：

- 高级参数区
- 机器创建 / 编辑页里的相关下拉项
- 任何当前把 `none` 渲染成“默认 / Default”的相关位置

要求：

- 值是 `none`，就显示 `none`
- 不要显示“默认”
- 不要显示 `Default`

原因：

- `none` 在这里是明确值，不只是“系统默认”的自然语言描述
- 用户要看到真实配置值，而不是被 UI 二次解释后的文案

## 不要做的事

- 不要把删除能力开放给所有内建参数
- 不要自行发明新的参数分类规则
- 不要把 `none` 再包装成更“友好”的词
- 不要顺手改坏已有的双向同步编辑行为

## 影响文件

你大概率会改：

- `src/components/QemuArgsList.tsx`
- `src/styles/app.css`
- `src/pages/MachineBuilderPage.tsx`
- `src/i18n/resources.ts`

## 验收点

完成后至少满足：

1. 每个可删参数项右侧都有垃圾桶按钮
2. 点击垃圾桶后，该条 custom 参数被删除
3. 不可删项没有垃圾桶
4. 相关 UI 里 `none` 显示为 `none`
5. 不影响现有高级参数区的展示和编辑逻辑

## 回写要求

请写 `kimi-sendback.md`，至少说明：

1. 你改了哪些前端文件
2. 垃圾桶按钮最终放在哪、什么状态显示
3. 哪些地方把“默认 / Default”改回了 `none`
4. 有没有顺手调整样式或交互细节
