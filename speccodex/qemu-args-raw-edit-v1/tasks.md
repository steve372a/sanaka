# Tasks

- [ ] 盘点当前哪些高级参数行仍走强绑定 controlled sync，哪些更适合切到 raw edit
- [ ] 明确“可关闭内建项”清单及其 `none` / `disabled` 回写目标
- [ ] 为完整命令项元数据补齐 `editable` / `removable` / `bindingKey` 的统一规则
- [ ] 将更多内建行从“值 token 编辑”升级为“整行原样编辑”
- [ ] 保留 `-m/-smp/-name` 这类简单直填参数的直接字段回写，不把它们送进“自定义”
- [ ] 设计并实现 UI “自定义”态的数据承接方式
- [ ] 收敛 `QemuArgsSync` 中不必要的智能解析范围
- [ ] 确保 `hostfwd` / `guestfwd` 等高级参数不会再被过度特殊处理到难以理解
- [ ] 允许关闭态如 `gpu = none` 正常参与命令展示刷新
- [ ] 为可关闭内建项接入删除语义
- [ ] 保证不可关闭骨架项没有删除入口
- [ ] 补充 raw edit 成功路径测试
- [ ] 补充 raw edit 非法参数由 QEMU 报错的测试或验收用例
- [ ] 更新前端协作文档，明确“自定义”态、垃圾桶、原样编辑边界
