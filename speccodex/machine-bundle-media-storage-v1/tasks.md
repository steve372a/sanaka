# Tasks

## Backend

- [ ] 保留 `WebWorkspaceService` 的 `Media + Disks` 固定沙盒边界。
- [ ] 将上传接口从任意目录字符串收紧为明确的资源用途。
- [ ] 为 `Media/` 和 `Disks/` 分别定义允许的文件类型与用途校验。
- [ ] 对 `.img` 使用调用入口和配置用途判断目标目录。
- [ ] 上传使用安全文件名、真实父目录校验和排他创建。
- [ ] 保证网页 API 不能列出机器包根目录或访问内部文件。
- [ ] 保持外部媒体和外部磁盘路径不向网页泄露。
- [ ] 保持 `ExportService` 将 ISO 写入 `Media/`、磁盘写入 `Disks/`。
- [ ] 不含介质时不创建空 `Media/`。
- [ ] 为导出资源增加重名避让，并把最终路径写回配置。
- [ ] 保持网页导出对外部宿主资源的拒绝逻辑。
- [ ] 更新响应清理，确保错误不泄露宿主路径。

## Frontend

- [ ] 保留“安装介质”和“虚拟磁盘”两个入口。
- [ ] 两个入口上传时分别传递明确的资源用途。
- [ ] 上传完成后刷新列表，不自动挂载或修改机器配置。
- [ ] 介质选择器只显示 `Media/` 中符合当前用途的文件。
- [ ] 磁盘选择器只显示 `Disks/` 中可作为虚拟硬盘的文件。
- [ ] 外部资源继续显示文件名，并保持不可修改提示。
- [ ] 不向用户显示机器包根路径或内部文件名。

## Tests

- [ ] 测试 ISO 和软盘上传到 `Media/`。
- [ ] 测试虚拟硬盘上传到 `Disks/`。
- [ ] 测试 `.img` 在硬盘和软盘用途下进入不同目录。
- [ ] 测试上传不会自动修改 `[media]` 或 `[[disks]]`。
- [ ] 测试 `machine.svm`、`preview.png` 和机器包根目录不可访问。
- [ ] 测试重名不会被覆盖。
- [ ] 测试路径穿越、Windows 盘符、符号链接和父目录替换逃逸被拒绝。
- [ ] 测试导出 ISO 位于 `Media/` 且配置路径正确。
- [ ] 测试不含 ISO 的导出不创建空 `Media/`。
- [ ] 测试网页导出仍拒绝外部宿主资源。
- [ ] 测试桌面端外部 ISO 和磁盘不受影响。
- [ ] 运行 `npm run typecheck`。
- [ ] 运行 `npm test -- --run`。
- [ ] 运行 `npm run build`。

## Documentation

- [ ] 更新 `docs/architecture/saka-format.md`，正式加入 `Media/`。
- [ ] 更新 `.svm、.saka的详细讲解.md`，说明 `Media/` 与 `Disks/` 的职责。
- [ ] 说明 `.img` 按用途归类，不能仅按扩展名判断。
- [ ] 实现完成后回写 `gpt-sendback.md`，说明安全边界和验证结果。
