# Tasks

- [ ] 在 `AppSettings` schema 和默认设置中增加 `showWelcomeOnStartup`。
- [ ] 确认 AppStore 加载旧设置时使用 schema 默认值，并保留保存失败状态。
- [ ] 抽取桌面版和网页版共用的欢迎窗口组件。
- [ ] 按现有弹窗体系实现遮罩、标题、圆形关闭按钮和底部操作区。
- [ ] 增加当前版本视频路径解析，版本号以 `package.json` 为准。
- [ ] 开发环境读取 `video/<version>.mp4`，缺失时回退黑色背景。
- [ ] 新增视频下载脚本，按 `<version>-beta` 到 `v<version>(beta)` 的规则生成 Release 下载地址。
- [ ] 下载器使用临时文件和原子改名，并正确处理 URL 中的圆括号。
- [ ] 把视频下载检查接入开发启动命令。
- [ ] 配置打包流程，将当前版本视频复制到应用资源目录下的 `video` 目录，确保安装包默认带视频。
- [ ] 把视频下载检查接入 macOS、Windows 和 Linux 打包命令。
- [ ] 让发布构建在下载失败或缺少当前版本视频时失败并输出明确地址与路径。
- [ ] 实现应用资源目录、用户数据目录缓存和 GitHub 下载的固定查找顺序。
- [ ] 用户删除内置视频后，将下载结果写入用户数据目录，不修改应用安装目录。
- [ ] 实现固定 16:9 圆角视频区域和黑色错误回退。
- [ ] 配置自动播放、无限循环、强制静音、`playsInline` 和无控件播放。
- [ ] 在 `.gitignore` 忽略 `video/*.mp4`，并保留目录说明文件。
- [ ] CI/发布流程先从同版本 Sanaka Release 下载视频，再构建安装包。
- [ ] 在桌面版主布局初始化后挂载欢迎窗口。
- [ ] 在网页版主布局初始化后挂载欢迎窗口。
- [ ] 实现 `关闭`、圆叉号和 `Escape` 的临时关闭行为。
- [ ] 实现“不再提醒”的持久化行为和失败处理。
- [ ] 补充焦点管理、ARIA 属性和键盘操作。
- [ ] 接入减弱动态效果设置，只保留淡入淡出。
- [ ] 编写组件测试和 AppStore/schema 测试。
- [ ] 在桌面版和网页版验证宽屏、窄屏、刷新、打开 `.saka`、视频缺失和解码失败场景。
- [ ] 运行 `npm run typecheck`、相关 Vitest、`npm run build` 和 `git diff --check`。
